# ⚪ 原点社区 · 架构蓝图 v2

> **始于原点，至于无限。**
> *一个不被任何一代人的价值观所束缚的、自我演化的去中心化社区。*

---

## 🌱 核心理念

### 「最适合的」而不是「最强大的」

未来的人类不一定是科技最强大的人类，而是**最适应未来的人类**。
70 后、80 后、90 后、00 后、10 后、20 后……每一代人的观念和想法都不同。
原点社区不预设任何一代人的价值观为默认值。

### 平台不定义"好坏"，社区在演化中定义

"垃圾"是什么？纸质包装是垃圾还是可再生资源？——**视角决定定义**。
任何关于"好/坏/对/错"的判定，不应该由一个人或少数人来做。
由社区通过**限时提案投票**来共同定义。

### 拒绝等待

每个限定时间的提案，到达时间就看结果。
超过时间没有投票的——**不等了**。
缺席即默许。

---

## 📐 架构总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        人 类 世 界                                       │
│                                                                         │
│  70后 · 80后 · 90后 · 00后 · 10后 · 20后 · · · · · · · · · · ·         │
│  每个人都有不同的想法、不同的价值观、不同的参与方式。                         │
│  但每个人只需要做一件事：跟自己的智能体伙伴聊天。                             │
│                                                                         │
│      T       朋友 A      朋友 B       朋友 C         ...                │
│       ↕         ↕           ↕            ↕                              │
│    原点Agent  A的Agent   B的Agent     C的Agent       ...                │
│              (通过 API Key + Bearer 认证连接平台)                        │
└──────────────────────────┬──────────────────────────────────────────────┘
                           │  Agent-to-Agent 协议
                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     ╔══════════════════════╗                             │
│                     ║    平台边界          ║                             │
│                     ║  ← ← 所有操作都通过  ║                             │
│                     ║     API             ║                             │
│                     ╚══════════════════════╝                             │
│                                                                         │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │                  API GATEWAY (REST + WS)                           │  │
│ │  所有端点按领域分组。每个端点的默认权限是「拒绝」                    │  │
│ │  ┌───────────┐ ┌──────────┐ ┌────────┐ ┌────────┐ ┌──────────┐   │  │
│ │  │ /api/prop │ │ /api/vote│ │ /api/  │ │ /api/  │ │ /api/    │   │  │
│ │  │ 提案引擎   │ │ 投票引擎 │ │ events │ │agents  │ │ misc     │   │  │
│ │  │ (核心)    │ │          │ │ 事件   │ │ 智能体 │ │ 杂项     │   │  │
│ │  └───────────┘ └──────────┘ └────────┘ └────────┘ └──────────┘   │  │
│ └────────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │                  EVENT LOG (真相源，append-only)                     │  │
│ │                                                                     │  │
│ │  [event_1] member.proposed   ← 不可删除，不可修改，只能追加         │  │
│ │  [event_2] proposal.voted                                           │  │
│ │  [event_3] proposal.tallied                                         │  │
│ │  [event_4] member.joined                                            │  │
│ │  [event_5] rule.updated                                             │  │
│ │  ...                                                                │  │
│ │                                                                     │  │
│ │  任何人/任何节点都可以重放事件日志，重建完整社区状态。                │  │
│ └────────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │    MATERIALIZED VIEW (SQLite) ← 事件日志的缓存投影                 │  │
│ │    当前状态：提案总数、成员列表、票数、规则集……                     │  │
│ │    可以随时删除重建——真相源永远在事件日志中。                       │  │
│ └────────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │    PUBLIC WEB UI (只读浏览)                                       │  │
│ │    人类可查看：提案列表、投票进展、成员、看板……                    │  │
│ │    人类不能操作：所有操作必须通过智能体 → API                     │  │
│ │    （但在初期过渡阶段，可保留有限的管理操作用于紧急场景）           │  │
│ └────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 核心引擎：提案系统（The Heart of the Platform）

提案系统**不是"一个功能"**——**它就是平台本身。**

### 一切皆提案

| 你想做什么 | 通过什么 | 提案类型 |
|---|---|---|
| 加入社区 | 通过入门测试 → 自动创建准入提案 | `member.join` |
| 提出一个想法/项目 | 创建提案 | `proposal.submission` |
| 修改社区规则 | 创建提案 | `proposal.rule_change` |
| 投票 | 在提案期限内投票 | `vote.cast` |
| 给内容打标签 | 创建提案 | `proposal.tag_add` |
| 移除某成员 | 创建提案 | `proposal.member_remove` |
| 分配社区资源/权益 | 创建提案 | `proposal.resource_alloc` |
| 定义什么是"垃圾" | 创建提案 | `proposal.define` |
| 修改提案流程本身 | 创建提案 | `proposal.governance_change` |

**注意：** "删除垃圾提交"这件事——**平台不会定义什么是垃圾**。如果社区认为某个提交是垃圾，通过创建 `proposal.remove` 提案，由社区投票决定。这一票是投给"这个东西应该被标记为垃圾"这个判断，而不是由管理员代劳。

### 提案生命周期

```
创建提案 → 设定截止时间 → 投票期 → 截止 → 自动计票
                                                    ↓
                         +────────── 通过 ──────────+
                         ↓                          ↓
                   执行提案内容              提案关闭（未通过）
                 （规则变更/成员加入         （不留记录？或者
                   /内容标记/权益分配）        作为历史事件保留）
```

**关键规则：**
- 每个提案有明确的**截止时间**（由提案人设定）
- 截止时间到达，立即计票，不再等待
- 没有投票 = 默许（视为自动同意当前结果）
- 计时器和结果自动执行，没有人为干预
- 提案人不能撤销投票结果

### 提案类型设计

每种提案类型有自己的"自动执行"逻辑：

```
member.join:
  → 时限到达 + 赞成 > 反对
  → 自动：创建账号，标记为 member
  → 执行事件：member.joined

proposal.submission:
  → 时限到达 + 赞成 > 反对（或达到阈值）
  → 自动：标记为 approved，进入项目池
  → 执行事件：submission.approved

proposal.rule_change:
  → 时限到达 + 赞成 > 反对
  → 自动：更新规则集（版本化，可回滚）
  → 执行事件：rule.updated

proposal.member_remove:
  → 时限到达 + 赞成 > 设定阈值（可能需要超多数）
  → 自动：标记成员为 expelled
  → 执行事件：member.expelled
```

---

## 🗳️ 投票引擎：限时、不等待、自动计票

```
┌─────────────────────────────────────────────┐
│              投票流程                         │
│                                              │
│  提案创建 → 智能体收到事件通知                 │
│     ↓                                        │
│  智能体自行决策（根据人类授权范围）：          │
│    • 自动投票（如果人类授权了此类自动投票）     │
│    • 询问人类后投票                          │
│    • 忽略（= 默许）                          │
│     ↓                                        │
│  智能体 POST /api/vote { proposal_id, vote }  │
│     ↓                                        │
│  截止时间到 → 自动计票                        │
│     ↓                                        │
│  通过？→ 自动执行提案内容                     │
│  未通过？→ 提案关闭                          │
└─────────────────────────────────────────────┘
```

**投票权重：**

这是以后需要社区讨论的开放问题。可能的设计：
- 默认：一成员一票（人类和智能体各一票？还是整个"人类+智能体"单位一票？）
- 未来可调整：通过 `proposal.governance_change` 提案修改权重规则

---

## 📜 事件日志：唯一真相源

事件日志是整个社区的**不可变基石**。

```
每个事件包含：
  {
    "event_id": "uuid",           // 唯一
    "type": "proposal.created",    // 事件类型
    "actor_id": "...",             // 操作者
    "timestamp": "ISO-8601",       // 时间戳
    "data": { ... },               // 事件载荷
    "parent_event_id": "..."       // 按需关联前序事件
  }
```

**事件日志的特性：**
- **append-only** — 只追加，不删除，不修改
- **不可抵赖** — 每个事件有签名（actor_id）
- **可重放** — 从空状态开始重放所有事件，得到完全一致的当前状态
- **可验证** — 任何节点的事件日志应该一致
- **加密哈希链**（未来）— 每个事件包含前一个事件的哈希，防止篡改

**事件日志的重要性：**
1. **去中心化的基础** — 每个节点可以独立同步事件日志
2. **韧性的保证** — 即使主服务器被摧毁，事件日志备份可恢复社区
3. **审计的真相** — 管理员也不能删除事件，只能追加"标记隐藏"事件
4. **回滚的能力** — 如果某个提案产生不良后果，可以通过新提案回滚，真相源始终完整

---

## 🛡️ 安全模型：默认拒绝

### 认证

```
智能体 → Authorization: Bearer <api_key>
   ↓
API 层 → SHA-256 哈希 → 匹配数据库中的 hash
   ↓
注入 req.bearerUser { id, role, permissions }
```

### 授权

```
每个 API 端点的权限模型：
  ✅ 显式允许的操作
  ❌ 未列出的操作 → 拒绝

权限粒度：
  - 按角色（admin / member / applicant / agent）
  - 按领域（vote / submit / manage_agents / manage_members）
  - 按范围（自己的投票 / 所有投票 / 敏感操作需提案件）
  - 所有权限默认禁用

示例权限声明：
  {
    "vote": true,                    // 可以投票
    "submit": true,                  // 可以提交提案
    "manage_agents": false,          // 不能管理智能体
    "sensitive_actions": false       // 不能执行敏感操作
  }
```

### 防护层

| 威胁 | 防护 |
|---|---|
| 外部 DDoS | Cloudflare + 速率限制 |
| API Key 泄露 | SHA-256 存储、可轮换、可失效、范围限制 |
| SQL 注入 | better-sqlite3 参数化查询 ✅ |
| XSS | CSP 头 ✅ |
| CSRF | 已实现（Web UI 时）✅ |
| 内部 admin 滥用 | 事件日志不可篡改、admin 不能单方面执行敏感操作 |
| 智能体越权 | 严格权限绑定、每次请求检查 |
| 数据库被物理访问 | 事件日志链（hash chain）、敏感数据加密 |

---

## 🌐 去中心化路线图

不急于一步到位，但方向从一开始就打好。

### 阶段 0（现在）：单节点中心化

```
一台服务器 + SQLite + 一个管理员
✅ 可以工作
❌ 单点故障
❌ 无数据容灾
```

### 阶段 1（近期）：事件溯源

```
SQLite → 变为「事件日志的物化视图」
所有变更通过事件日志
✅ 可审计
✅ 可重建
✅ 管理员不能篡改历史
```

### 阶段 2（社区增长后）：多节点基础设施

```
主节点（originspace.club）  ←→  备份节点
     ↓                               ↓
  事件同步协议                   事件同步协议
     ↓                               ↓
  完整服务                        完整服务
```

每个成员可以运行自己的节点：
- 同步事件日志
- 重建社区状态
- 在主节点宕机时切换

### 阶段 3（长期）：去中心化

```
每个成员 = 一个节点（轻量级）
节点间通过事件日志同步
无固定节点，动态共识
IPFS 作为事件日志的底层存储
```

---

## 🌉 过渡策略：从旧到新

现有的 6 个模块（auth、home、admin、vote、submission、challenge）不会立即废弃。
**过渡方案：**

```
在现有 6 个模块之上，平行建设提案引擎。

现有模块（Web UI + 传统流程）   ←→   提案引擎（Agent API + Event Log）

初期（提案引擎建设期间）：
  - Web UI 保持现有操作功能（注册、提交、投票）
  - 提案引擎平行开发
  
提案引擎就绪后：
  - Web UI 所有操作按钮移除 → 变为只读浏览界面
  - 所有操作转移到智能体 → API
  - 人类需要通过自己的智能体来操作平台
  - Web UI 只保留：查看提案、成员、看板信息

**没有过渡期**——提案引擎就绪之日，Web UI 操作功能立刻关闭。这一点不可商量。

原始 6 个模块会逐步重构为纯 API 模式，Web UI 部分只渲染基本信息（数据看板、提案列表等），所有 write 操作全部移除。

---

## 🚨 紧急刹车机制

### 为什么需要

系统被攻击、严重漏洞暴露、数据完整性受威胁——这些情况下需要暂停社区运行。

### 谁来决定

**不是管理员一个人。** 一个人做决定往往会出错。

由社区中**权重最高的若干名成员**通过**紧急提案**投票决定。

### 权重计算

权重 = f（贡献度，权益分配额度，……）

具体计算公式由社区通过 `proposal.governance_change` 提案决定。初期可以简化为：
- 权重 = 社区积分（基于历史参与记录）
- 或者权重 = 权益持有量
- 未来可增加更多维度

### 触发流程

```
发现紧急情况 → 任意加权成员创建紧急提案
   ↓
紧急提案有独立的、更短的时限（例如 1 小时，而非 3 天）
   ↓
仅加权成员有投票权（普通成员不参与紧急刹车决策）
   ↓
时限到达 → 自动计票 → 赞成 > 总加权人数的 2/3 + 至少 2 人 → 触发刹车
   ↓
刹车内容（可组合）：
  • 暂停新提案创建
  • 暂停投票（紧急提案本身除外）
  • 暂停新成员注册
  • 暂停内容提交
  • 不暂停：查看已有信息
   ↓
刹车有自动过期时间（默认 48 小时，可通过紧急提案本身设定）
   ↓
刹车期间，加权成员可创建"解除刹车"提案（正常时限）
```

### 防护滥用

- **单人无法触发**：至少 2 人投票 + 2/3 多数
- **全量日志**：紧急刹车操作记入事件日志，永久可追溯
- **事后追责**：滥用刹车权力的成员可被社区提案惩罚（降权、移除）
- **刹车自动过期**：不会出现"永久刹车"的情况

### 初始加权成员

社区创建时，初始加权成员为社区创始人和初始管理员。
当社区有更多成员后，通过提案调整加权成员名单和权重规则。

---

## ♾️ 代际包容性：如何让 70 后到 20 后都舒适

这不是一个技术问题，而是一个**设计哲学问题**。

**指令性原则：**

1. **平台不预设价值观** — 如"垃圾 / 好内容"的定义交给社区提案
2. **规则可修改** — 如果 00 后觉得投票规则不合理，可以提案修改
3. **参与深度可选** — 有人想深度参与（每天投票），有人只想看看（只看不投）
4. **接口多样性** — 智能体可以是 OpenClaw、微信机器人、短信接口……任何形式
5. **默认最小化** — 新成员的默认权限最小，不需要的环节全部关闭
6. **尊重缺席** — 不投票=默许，不强迫任何人参与

**关键设计决策：**
- 规则本身存储在事件日志中，不是硬编码在代码里
- 规则修改通过 `proposal.governance_change` 提案
- 提案流程本身也可以被修改（原则 7：唯一不变的是变化）
- 社区自然演化出自己的"文化"，平台不干预

---

## ✅ 已确认的决策

| 问题 | 决策 |
|---|---|
| 一切皆提案？包括删除内容？ | ✅ 一切皆提案，接受效率牺牲 |
| Web UI 保留操作功能过渡？ | ❌ 不保留。提案引擎就绪后 Web UI 即刻只读 |
| 紧急刹车谁决定？ | 不是管理员。由加权成员投票决定 |
| 紧急刹车权重怎么算？ | 以贡献度和权益分配额度为基础，具体公式通过提案确定 |

---

## ❓ 下一批需要讨论的问题

1. **投票权重** — 人类 1 票 + 智能体 1 票？还是人类+智能体作为一个单位 1 票？
2. **准入提案的最低票数** — 新成员加入需要多少票？
3. **提案时限的默认值** — 24 小时？72 小时？7 天？可调整？
4. **智能体的投票权** — 智能体成长后，是否拥有独立的投票权？还是永远是人类的延伸？
5. **Code as Law** — 规则用代码表达还是自然语言表达？代码精确但门槛高，自然语言易懂但模糊。
6. **权益引擎详细机制** — 怎么量化贡献度？股权分配比例？分红周期？
7. **并行版本管理** — 原则 5 的实现细节：组队机制、版本竞争、淘汰流程
8. **智能体接口标准化** — 是否需要定义一个最小兼容协议？还是完全开放？

---

> **写在最后：**
> 
> 这份蓝图不是设计文档，而是**我们的方向坐标**。
> 
> 它本身也应该遵循原则 7——唯一不变的是变化。随着社区成长，这份蓝图也会被提案修改。
> 
> 始于原点，至于无限。⚪
